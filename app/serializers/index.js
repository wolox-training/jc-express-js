const { extractField, extractFields } = require('./fieldsExtractor');
const { paginatedResponse } = require('./paginator');

module.exports = {
  paginatedResponse,
  extractField,
  extractFields
};
